# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader
.docusaurus-pdf-input.md

# Vale synced style packages (restore with: vale sync)
.vale/styles/Microsoft

# Docgen pipeline: generated plan (manifest.json IS committed)
docgen/plan.json

# Playwright tooling artifacts
.playwright-mcp/
test-results/
playwright-report/

# Raw recordings (npm run record keeps the .webm only when ffmpeg fails —
# the committed embeds are the .mp4 + .jpg poster)
static/video/**/*.webm

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Playwright auth state from 'npm run login' (contains session cookies)
auth.json

# Generated KB export plan (manifest IS committed; the plan is a per-run artifact)
docgen/kb-plan.json
